// startScreen() {
//         if (!game.gameStarted) {
//             // document.querySelector(‘#start > span’).innerText = ‘press  `ENTER` to start.’
//             image(game.startScreenImage, 0, 0, width, height);
//         } else {
//             // document.querySelector(‘#start > span’).innerText = ‘’
//         }
//     }